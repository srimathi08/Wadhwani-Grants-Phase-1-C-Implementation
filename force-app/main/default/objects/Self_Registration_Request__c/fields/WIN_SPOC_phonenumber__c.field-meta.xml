<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>WIN_SPOC_phonenumber__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    AND(NOT(ISPICKVAL(WIN_SPOC_C__c, &quot;&quot;)), NOT(ISBLANK(WIN_SPOC_Phone_Number__c))),
    TEXT(WIN_SPOC_C__c) &amp; &quot; &quot; &amp; WIN_SPOC_Phone_Number__c,
    IF(
        NOT(ISPICKVAL(WIN_SPOC_C__c, &quot;&quot;)),
        TEXT(WIN_SPOC_C__c),
        WIN_SPOC_Phone_Number__c
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>WIN SPOC phone number</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
